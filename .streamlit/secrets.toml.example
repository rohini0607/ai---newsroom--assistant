# Rename this file to "secrets.toml" (keep it inside the .streamlit folder)
# if deploying to Streamlit Community Cloud. Do NOT commit the real
# secrets.toml file to GitHub.

GEMINI_API_KEY = "your_gemini_api_key_here"
